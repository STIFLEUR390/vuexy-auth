    <!-- Hero -->
    <div class="bg-body-light">
        <div class="content content-full">
            <div class="d-flex flex-column flex-sm-row justify-content-sm-between align-items-sm-center py-2">
                <div class="flex-grow-1">
                    <h1 class="h3 fw-bold mb-2">
                        @lang("Users Index")
                    </h1>
                    {{-- <h2 class="fs-base lh-base fw-medium text-muted mb-0">
            Carefully designed elements that will ensure a great experience for your users.
          </h2> --}}
                </div>
                <nav class="flex-shrink-0 mt-3 mt-sm-0 ms-sm-3" aria-label="breadcrumb">
                    <ol class="breadcrumb breadcrumb-alt">
                        <li class="breadcrumb-item">
                            <a class="link-fx" href="javascript:void(0)">@lang('User')</a>
                        </li>
                        <li class="breadcrumb-item" aria-current="page">
                            @lang('Role')
                        </li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
    <!-- END Hero -->

    <!-- Page Content -->
    <div class="content">
        <!-- Basic -->
        <div class="block block-rounded">
            <div class="block-header block-header-default">
                <h3 class="block-title">
                    <a href="{{ route('admin.users.index') }}">
                        <i class="fa fa-arrow-left-long"></i>
                        @lang('return')
                    </a>
                </h3>
            </div>
            <div class="block-content block-content-full">
                <div class="table-responsive">
                    <table class="table table-bordered table-striped table-vcenter">
                      <tbody>
                        <tr>
                          <td class="text-center">
                              @lang('Name')
                          </td>
                          <td class="fw-semibold fs-sm">
                            {{ Auth::user()->name }}
                          </td>
                        </tr>
                        <tr>
                          <td class="text-center">
                              @lang('Email')
                          </td>
                          <td class="fw-semibold fs-sm">
                            {{ Auth::user()->email }}
                          </td>
                        </tr>
                      </tbody>
                    </table>
                  </div>
            </div>
        </div>
        <!-- END Basic -->

        @if ($roles)
            <div class="block block-rounded">
                <div class="block-header block-header-default">
                    <h3 class="block-title">@lang('Roles')</h3>
                </div>
                <div class="block-content block-content-full">
                    @if (Auth::user()->roles)
                        <ul>
                            @foreach (Auth::user()->roles as $key => $role)
                                <li>
                                    <div class="row mt-1">
                                        <div class="col-3">{{ $role->name }}</div>
                                        <div class="col-3">
                                            <form method="POST" id="formrol{{ $key + 1 }}"
                                                action="{{ route('admin.users.roles.remove', ['role' => $role->id, 'user' => Auth::user()->id]) }}"
                                                style="display: none">
                                                @csrf
                                                @method('DELETE')
                                            </form>
                                            <button type="button" onclick="confirmDelete('rol'+{{ $key + 1 }})"
                                                class="mx-2 btn btn-sm btn-alt-secondary js-bs-tooltip-enabled"
                                                data-bs-toggle="tooltip" title="@lang('Delete')" data-bs-original-title="@lang("
                                                Delete")">
                                                <i class="fa fa-fw fa-times"></i>
                                            </button>
                                        </div>
                                    </div>
                                </li>
                            @endforeach
                        </ul>
                    @endif

                    <form method="POST" class="mt-2" action="{{ route('admin.users.roles', Auth::user()->id) }}">
                        @csrf
                        <div class="row push">
                            <div class="col-lg-4">
                                <p class="fs-sm text-muted">
                                    @lang('Select role to assign the user')
                                </p>
                            </div>
                            <div class="col-lg-8 col-xl-5">
                                <div class="mb-4">
                                    <label class="form-label" for="role">{{ __('Role') }}</label>
                                    <select class="form-select" id="role" name="role">
                                        @foreach ($roles as $roles)
                                            <option value="{{ $roles->name }}">{{ $roles->name }}</option>
                                        @endforeach
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="mb-4">
                            <button type="submit" class="btn btn-dark">{{ __('Assign') }}</button>
                        </div>
                    </form>
                </div>
            </div>
        @endif

        @if ($permissions)
            <div class="block block-rounded">
                <div class="block-header block-header-default">
                    <h3 class="block-title">@lang('Permissions')</h3>
                </div>
                <div class="block-content block-content-full">
                    @if (Auth::user()->permissions)
                        <ul>
                            @foreach (Auth::user()->permissions as $keys => $permission)
                                <li>
                                    <div class="row mt-1">
                                        <div class="col-3">{{ $permission->name }}</div>
                                        <div class="col-3">
                                            <form method="POST" id="formperm{{ $keys + 1 }}"
                                                action="{{ route('admin.users.permissions.revoke', ['permission' => $permission->id, 'user' => Auth::user()->id]) }}"
                                                style="display: none">
                                                @csrf
                                                @method('DELETE')
                                            </form>
                                            <button type="button" onclick="confirmDelete('perm'+{{ $keys + 1 }})"
                                                class="mx-2 btn btn-sm btn-alt-secondary js-bs-tooltip-enabled"
                                                data-bs-toggle="tooltip" title="@lang('Delete')" data-bs-original-title="@lang("
                                                Delete")">
                                                <i class="fa fa-fw fa-times"></i>
                                            </button>
                                        </div>
                                    </div>
                                </li>
                            @endforeach
                        </ul>
                    @endif

                    <form method="POST" class="mt-2" action="{{ route('admin.users.permissions', Auth::user()->id) }}">
                        @csrf
                        <div class="row push">
                            <div class="col-lg-4">
                                <p class="fs-sm text-muted">
                                    @lang('Select permission to assign the user')
                                </p>
                            </div>
                            <div class="col-lg-8 col-xl-5">
                                <div class="mb-4">
                                    <label class="form-label" for="permission">{{ __('Permission') }}</label>
                                    <select class="form-select" id="permission" name="permission">
                                        @foreach ($permissions as $permission)
                                            <option value="{{ $permission->name }}">{{ $permission->name }}</option>
                                        @endforeach
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="mb-4">
                            <button type="submit" class="btn btn-dark">{{ __('Assign') }}</button>
                        </div>
                    </form>
                </div>
            </div>
        @endif
    </div>
    <!-- END Page Content -->
